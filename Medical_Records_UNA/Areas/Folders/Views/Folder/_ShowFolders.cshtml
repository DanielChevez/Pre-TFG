@using Medical_Records_Utilities.Extensions;



@{
    ViewData["Title"] = "Expedientes";
    //List<UsuarioViewModel> lstUsuarios = (List<UsuarioViewModel>)ViewBag.Usuarios;
    List<int> Actions = Context.Session.GetObject<List<int>>("ActionsOfUser");
    if (Actions == null)
    {
        Actions = new List<int>();
    }
}

<div class="header_page">

    <!-- TITULO DE LA PAGINA-->


    <div>
        <h3 class="m-1">Expedientes</h3>
        <select class="custom-select m-2" aria-label="Cantidad de registros a mostrar" id="tamanoPagina" name="tamanoPagina">
            <option value="10">10</option>
            <option value="20">20</option>
            <option value="50">50</option>
        </select>

      @*   @if (Actions.Contains(4))
        { *@
            <button type="button" class="btn btn-primary m-2 " id="create_patient">
                <i class="fa-solid fa-user-plus"></i>
                Agregar Paciente
            </button>

        @* } *@
    </div>
@* 
    <div class="input-group  ">
        <input id="buscar" type="search" class="form-control rounded" placeholder="Buscar" aria-label="Buscar" aria-describedby="search-addon"
               value="@ViewBag.palabraBuscar" />
        <span class="input-group-text border-0" id="search-addon">
            
        </span>
    </div> *@
    <div class="search m-2 rounded">
        <input id="buscar" type="text" placeholder="Buscar..."
               class="form-control" aria-label="Buscar..."
               value="@ViewBag.palabraBuscar" />
        <span class="input-group-text border-0" id="search-addon">
            <i class='bx bx-search-alt bx-tada'></i>
        </span>
        @* <span class="tooltip"><i class="fas fa-search"></i>Buscar</span> *@
    </div>




</div>


<div class="overflow-y-scroll  table_render scroll " id="foldersTable">
    <partial name="_TableFolders" />
</div>